<div class="container-fluid container-fluid-custom pb-5 mb-5">
  <div class="row mb-4">
    <div class="col">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          <li class="breadcrumb-item">
            <a href="/dashboard">Strona główna</a>
          </li>
          <li class="breadcrumb-item active-1">
            <%= note.title %>
          </li>
        </ol>
      </nav>
      <div class="col d-flex justify-content-between align-items-center">
        <h1 class="h2">Przeglądanie notatki</h1>
        <button type="button" class="btn btn-danger me-2" data-bs-toggle="modal" id="deleteButton" data-bs-target="#deleteModal">
          Usuń notatkę
        </button>
      </div>
    </div>
  </div>

  <form action="/dashboard/item/<%= noteID %>?_method=PUT" method="POST" class="position-relative">
    <div class="form-group mb-4 position-absolute" style="left:1px;top:1px;right:1px;">
      <input type="text" class="form-control fw-bold border-0 fs-4" id="title" name="title" value="<%= note.title %>" placeholder="Tytuł notatki">
    </div>

    <div class="form-group mb-4">
      <textarea class="form-control pt-5 fs-4" id="body" name="body" placeholder="Napisz notatkę..." rows="12"><%= note.body %></textarea>
    </div>

    <div class="form-group">
      <button type="submit" class="btn btn-primary btn-lg">Zapisz</button>
    </div>
  </form>

</div>

<div class="modal" tabindex="-1" role="dialog" id="deleteModal">
  <div class="modal-dialog" role="document">
    <div class="modal-content rounded-4 shadow">
      <div class="modal-header border-bottom-0">
        <h1 class="modal-title fs-5">Usuwanie notatki</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body py-0">
        <p>Właśnie zamierzasz usunąć notatkę. Czy jesteś pewien?</p>
      </div>
      <div class="modal-footer flex-column align-items-stretch w-100 gap-2 pb-3 border-top-0">
        <form action="/dashboard/item/<%= noteID %>?_method=DELETE" method="POST" class="position-relative">
          <button type="submit" class="btn btn-lg btn-primary" style="width:100%">Tak, usuń notatkę</button>
        </form>
        <button type="button" class="btn btn-lg btn-secondary" data-bs-dismiss="modal">Anuluj</button>
      </div>
    </div>
  </div>
</div>
